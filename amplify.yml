# Plain HTML/JS static site — no npm build.
# Site root is public-static/; index.html is served at the app URL.
version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static site — no build step"
  artifacts:
    baseDirectory: public-static
    files:
      - "**/*"
